# JWT secret used to sign authentication tokens (replace with a secure random value in production)
JWT_SECRET=replace_this_with_a_strong_secret

# NEXT_PUBLIC_* variables used previously for Firebase are no longer required for local SQLite auth
