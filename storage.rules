rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    
    // Event posters can be read by anyone, but only created by authenticated users.
    match /events/{allPaths=**} {
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}
