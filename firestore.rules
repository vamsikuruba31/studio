rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    
    // Allow any authenticated user to read/write to users, events, and registrations
    match /{collection}/{document} 
      where collection in ['users', 'events', 'registrations'] {
      allow read, write: if request.auth != null;
    }
  }
}
